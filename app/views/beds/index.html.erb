<h1>Listing beds</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @beds.each do |bed| %>
  <tr>
    <td>Status: <%= bed.field_state %></td>
    <td><%= link_to 'Show', bed %></td>
    <td><%= link_to 'Edit', edit_bed_path(bed) %></td>
    <td><%= link_to 'Destroy', bed, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bed', new_bed_path %>
