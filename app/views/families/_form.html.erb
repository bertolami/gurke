<%= form_for(@family) do |f| %>
  <% if @family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@family.errors.count, "error") %> prohibited this family from being saved:</h2>

      <ul>
      <% @family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :energy %> <br />
    <%= select "family", "energy", @possible_energies%>
  </div>
  <div class="field">
    <%= f.label :field_state %><br />
    <%= select "family", "field_state", @possible_states%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
